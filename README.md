# Agent Process Engine – Test Skeleton

## Setup rápido

```bash
python -m venv venv
source venv/bin/activate  # ou venv\Scripts\activate no Windows
pip install -r requirements.txt
```

Este projeto contém apenas o **núcleo testável do AgentFactory**,
sem dependência de banco real ainda.

Próximo passo: integrar Django + ORM.